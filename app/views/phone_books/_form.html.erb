<%= form_with(model: phone_book, local: true) do |form| %>
  <% if phone_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone_book.errors.count, "error") %> prohibited this phone_book from being saved:</h2>

      <ul>
      <% phone_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contact %>
    <%= form.text_field :contact, id: :phone_book_contact %>
  </div>

  <div class="field">
    <%= form.label :phoneNumber %>
    <%= form.text_field :phoneNumber, id: :phone_book_phoneNumber %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
